# Supabase Configuration
# Create a file named .env.local with these values
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY

# Steps to set up your environment:
# 1. Create a .env.local file in the frontend directory
# 2. Copy the above variables into the file
# 3. Replace YOUR_PROJECT_REF and YOUR_ANON_KEY with your actual Supabase project values
# 4. These values can be found in your Supabase dashboard under Settings > API

# API Configuration (for hybrid approach)
NEXT_PUBLIC_API_URL=http://localhost:8080 