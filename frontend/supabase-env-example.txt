# Supabase Configuration - REQUIRED FOR PASSWORD RESET
# Create a file named .env.local with these values
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Steps to set up your environment:
# 1. Create a .env.local file in the frontend directory
# 2. Copy the above variables into the file
# 3. Replace YOUR_PROJECT_REF and YOUR_ANON_KEY with your actual Supabase project values
# 4. Get YOUR_SERVICE_ROLE_KEY from Supabase Dashboard > Settings > API > service_role key
# 5. These values can be found in your Supabase dashboard under Settings > API

# IMPORTANT: SUPABASE_SERVICE_ROLE_KEY is REQUIRED for password reset to work!

# Build Troubleshooting:
# If you get build errors, try:
# 1. npm install (ensure all dependencies are installed)
# 2. npm run lint (check for code issues)
# 3. rm -rf .next && npm run build (clean build)
# 4. Check that all imports are correct and files exist

# API Configuration (for hybrid approach)
NEXT_PUBLIC_API_URL=http://localhost:8080 